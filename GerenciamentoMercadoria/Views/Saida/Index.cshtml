@model List<Saida>
@{
    ViewData["Title"] = "Saida";
}

<div class="text-left">

    @if (TempData["Sucesso"] != null)
    {
        <div class="alert alert-success" role="alert">
            <button type="button" class="close-alert text-success" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <strong>Sucesso! </strong>@TempData["Sucesso"]
        </div>
    }
    @if (TempData["Falha"] != null)
    {
        <div class="alert alert-danger" role="alert">
            <button type="button" class="close-alert text-danger" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <strong>Error! </strong>@TempData["Falha"]
        </div>
    }

    <h4>Lista de Saidas</h4>
    <div class="pt-4">
        <a role="button" class="btn btn-danger" asp-controller="Saida" asp-action="Inserir">Adicionar</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Tipo</th>
                <th scope="col">Data / Hora</th>
                <th scope="col">Produto</th>
                <th scope="col">Quantidade</th>
                <th scope="col">Estado(UF)</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (Saida Saida in Model)
                {
                    <tr>
                        <th scope="row">@Saida.Id</th>
                        <td>Saida</td>
                        <td>@Saida.DataHora</td>
                        <td>@Saida.mercadoria.Nome</td>
                        <td>@Saida.Quantidade</td>
                        <td>@Saida.Estado</td>
                        <td>
                            <div class="btn-group" role="group" aria-label="Basic example">
                                <a role="button" asp-route-id="@Saida.Id" asp-controller="Saida" asp-action="Editar" class="btn btn-primary">Editar</a>
                                <a role="button" asp-route-id="@Saida.Id" asp-controller="Saida" asp-action="Confirmacao" class="btn btn-danger">Excluir</a>
                            </div>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>

</div>


